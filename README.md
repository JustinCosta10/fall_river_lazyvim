# Fall River LazyVim

My Neovim configuration using the **rose-pine** theme with many major **LSPs** configured.

## Installation

Clone the repository over your existing Neovim config and sync plugins:

```bash
git clone https://github.com/JustinCosta10/fall_river_lazyvim ~/.config/nvim
nvim --headless "+Lazy! sync" +qa

